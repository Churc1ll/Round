const cir=document.querySelector('.circ');
const tlm=new TimelineMax({});
cir.addEventListener('click',_=>{
  tlm
  .to(cir,1,{scale:1.5})
})
if (cir,1,{scale:1.5})
{
  cir.addEventListener('click',_=>{
    tlm
    .to(cir,1,{scale:1})
  })
}
